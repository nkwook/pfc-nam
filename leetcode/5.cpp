#include <iostream>
using namespace std;

class Solution {
public:
    string longestPalindrome(string s) {
        
    }
};

int main()
{
    Solution s;

    // ListNode *l1=new ListNode(2, new ListNode(4, new ListNode(3)));
    // ListNode *l2=new ListNode(5, new ListNode(6, new ListNode(4)));
    // vector<int> vin = {1, 2, 3};
    string s = s.longestPalindrome("babad");
    cout << s;
    return 0;
}